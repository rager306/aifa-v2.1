# Performance Workflow — Комплексная оптимизация

Практический гайд по регулярному мониторингу и оптимизации производительности в проекте AIFA v2.1 с использованием Bundle Analyzer и Lighthouse CI.

## 🎯 Цели workflow

1. **Проактивный мониторинг**: Отслеживание изменений производительности в реальном времени
2. **Превентивные меры**: Выявление проблем до попадания в production
3. **Измеримые улучшения**: Фиксация результатов оптимизаций с конкретными цифрами
4. **Документирование знаний**: Создание базы знаний по оптимизации

## 📊 Интеграция с инструментами проекта

### Связанные инструменты

| Инструмент | Роль в workflow | Когда использовать |
|------------|-----------------|-------------------|
| **Bundle Analyzer** | Анализ размеров бандлов | После добавления зависимостей |
| **Lighthouse CI** | Performance аудиты | Перед каждым релизом |
| **knip** | Выявление неиспользуемого кода | Еженедельно |
| **Snyk** | Безопасность зависимостей | Перед обновлениями |
| **Playwright** | E2E тесты производительности | После оптимизаций |
| **Vercel Analytics** | Real-user monitoring | Постоянно |

## 🔄 Регулярные проверки

### Ежедневная разработка

```bash
# При добавлении новой зависимости
npm run analyze  # Проверить размер бандла
npm run lighthouse  # Проверить performance

# Если рост >10% — исследовать причины
npm run knip  # Найти неиспользуемый код
```

### Перед коммитом

```bash
# Чеклист оптимизации
npm run analyze  # ✅ Размер бандла в норме
npm run lighthouse:ci  # ✅ Performance ≥90
npm run biome:fix  # ✅ Форматирование
npm run test:run  # ✅ Тесты проходят
```

### Перед релизом

```bash
# Полный аудит производительности
npm run analyze:both  # Клиентский + серверный бандл
npm run lighthouse:ci  # Desktop аудит
npm run lighthouse:mobile  # Mobile аудит

# Проверка трендов
git diff main  # Сравнить с baseline
```

## 📋 Чеклист оптимизации производительности

### ✅ Анализ бандлов

- [ ] **Размер клиентского бандла** < 500 KB gzipped
- [ ] **Размер серверного бандла** < 200 KB gzipped
- [ ] **Нет дубликатов зависимостей** (например, две версии React)
- [ ] **Нет неиспользуемых зависимотей** (`npm run knip`)
- [ ] **Tree-shaking работает** (lodash/debounce вместо lodash)

### ✅ Lighthouse аудит

- [ ] **Performance** ≥ 90 для всех страниц
- [ ] **Accessibility** ≥ 95 для всех страниц
- [ ] **Best Practices** ≥ 90 для всех страниц
- [ ] **SEO** ≥ 95 для всех страниц
- [ ] **FCP** ≤ 1.8s для всех страниц
- [ ] **LCP** ≤ 2.5s для всех страниц
- [ ] **CLS** ≤ 0.1 для всех страниц
- [ ] **TBT** ≤ 200ms для всех страниц

### ✅ Core Web Vitals

- [ ] **LCP оптимизирован**
  - Hero image с `priority` и правильными размерами
  - Preload критичных ресурсов
  - Оптимизирован font loading

- [ ] **CLS минимизирован**
  - Резервирование места для динамического контента
  - Размеры изображений указаны явно
  - Font display: swap

- [ ] **FID/TBT оптимизирован**
  - Code splitting для тяжелых компонентов
  - Минимум синхронного JavaScript
  - Lazy loading вне экрана

## 🔍 Типичные проблемы и решения

### Проблема 1: Большой бандл

**Симптомы**:
```bash
npm run analyze
# main.js: 450 KB gzipped ⚠️
```

**Диагностика**:
```bash
# Найти самые большие модули
open .next/analyze/index.html

# Проверить неиспользуемый код
npm run knip:report
```

**Решения**:

1. **Code Splitting**
   ```typescript
   const HeavyChart = dynamic(() => import('./HeavyChart'), {
     loading: () => <ChartSkeleton />
   });
   ```

2. **Замена библиотек**
   ```typescript
   // ❌ moment (65 KB)
   import moment from 'moment';

   // ✅ dayjs (2 KB)
   import dayjs from 'dayjs';
   ```

3. **Tree-shaking**
   ```typescript
   // ❌ lodash (1 MB)
   import _ from 'lodash';

   // ✅ es-toolkit (5 KB)
   import debounce from 'es-toolkit/compat/debounce';
   ```

### Проблема 2: Низкий Performance score

**Симптомы**:
```bash
npm run lighthouse
# Performance: 78 ⚠️
# LCP: 3.2s ❌
```

**Диагностика**:
```bash
# Открыть детальный отчет
npm run lighthouse:report

# Проверить Network tab в DevTools
```

**Решения**:

1. **Оптимизация LCP**
   ```typescript
   <Image
     src="/hero.webp"
     alt="Hero"
     width={1920}
     height={1080}
     priority
     sizes="100vw"
     style={{ width: '100%', height: 'auto' }}
   />
   ```

2. **Предзагрузка критичных ресурсов**
   ```typescript
   // В layout.tsx
   <Head>
     <link rel="preload" href="/hero.webp" as="image" />
     <link rel="preload" href="/font.woff2" as="font" type="font/woff2" crossorigin />
   </Head>
   ```

3. **Критичный CSS**
   ```typescript
   // Inline критичных стилей
   <style dangerouslySetInnerHTML={{ __html: criticalCSS }} />
   ```

### Проблема 3: Высокий CLS

**Симптомы**:
```bash
npm run lighthouse
# CLS: 0.25 ❌
```

**Решения**:

1. **Резервирование места**
   ```typescript
   <div style={{ minHeight: content ? 'auto' : '200px' }}>
     {content ? <DynamicContent /> : <Skeleton />}
   </div>
   ```

2. **Размеры изображений**
   ```typescript
   <img
     src="image.jpg"
     width={100}
     height={100}
     alt="Description"
   />
   ```

3. **Стабильные шрифты**
   ```css
   body {
     font-family: 'Inter', system-ui, sans-serif;
     font-display: swap;
   }
   ```

## 📈 Пример оптимизации с измеримыми результатами

### Исходное состояние

```bash
# Bundle Analyzer
npm run analyze
# main.js: 450 KB gzipped

# Lighthouse
npm run lighthouse
# Performance: 78
# LCP: 3.2s
# CLS: 0.25
```

### Шаг 1: Code Splitting

```typescript
// Заменить статический импорт
// import HeavyChart from './HeavyChart';

// На динамический
const HeavyChart = dynamic(() => import('./HeavyChart'));
```

**Результат**:
```bash
npm run analyze
# main.js: 280 KB gzipped (-170 KB, -38%)

npm run lighthouse
# Performance: 85 (+7)
# LCP: 2.8s (-0.4s)
```

### Шаг 2: Оптимизация изображений

```typescript
<Image
  src="/hero-optimized.webp"
  width={1920}
  height={1080}
  priority
  sizes="100vw"
/>
```

**Результат**:
```bash
npm run lighthouse
# Performance: 92 (+7)
# LCP: 2.1s (-0.7s)
```

### Шаг 3: Резервирование места

```typescript
<div style={{ minHeight: '60px' }}>
  {showBanner && <Banner />}
</div>
```

**Результат**:
```bash
npm run lighthouse
# CLS: 0.05 (-0.20)
```

### Финальное сравнение

| Метрика | До | После | Улучшение |
|---------|----|----|-----------|
| **Bundle Size** | 450 KB | 280 KB | -38% |
| **Performance** | 78 | 92 | +18% |
| **LCP** | 3.2s | 2.1s | -34% |
| **CLS** | 0.25 | 0.05 | -80% |

## 🔗 Интеграция с Vercel

### Автоматический мониторинг

Vercel предоставляет встроенные инструменты мониторинга:

1. **Bundle Size Tracking**
   - Автоматические комментарии в PR
   - История изменений
   - Сравнение с предыдущими деплоями

2. **Vercel Analytics**
   - Real User Monitoring (RUM)
   - Core Web Vitals от реальных пользователей
   - Географическое распределение

3. **Speed Insights**
   - Детальная аналитика производительности
   - Тренды по времени
   - Рекомендации по улучшению

### Настройка алертов

```javascript
// vercel.json
{
  "functions": {
    "app/api/**/*.js": {
      "maxDuration": 10
    }
  }
}
```

## 📊 Документирование результатов

### Шаблон отчета об оптимизации

```markdown
## Оптимизация [дата]

### Проблема
[Описание проблемы с метриками]

### Решение
[Что было сделано]

### Результат
| Метрика | До | После | Улучшение |
|---------|----|----|-----------|
| Bundle Size | XXX KB | XXX KB | -XX% |
| Performance | XX | XX | +XX |
| LCP | X.Xs | X.Xs | -XX% |

### Выводы
[Что learned, что можно улучшить дальше]
```

### Ведение журнала изменений

```bash
# Создать отчет о baseline
npm run analyze > bundle-baseline-$(date +%Y%m%d).txt
npm run lighthouse > lighthouse-baseline-$(date +%Y%m%d).txt

# Сохранить в Git
git add bundle-baseline-*.txt lighthouse-baseline-*.txt
git commit -m "docs: performance baseline $(date +%Y%m%d)"
```

## 🚀 Продвинутые техники

### A/B тестирование оптимизаций

```bash
# Ветка с оптимизацией
git checkout -b feature/optimize-lcp
# ... внести изменения ...
npm run lighthouse > results-optimized.json

# Ветка baseline
git checkout main
npm run lighthouse > results-baseline.json

# Сравнить результаты
diff results-baseline.json results-optimized.json
```

### Автоматизация в Git Hooks (опционально)

```yaml
# .lefthook.yml
pre-push:
  commands:
    performance-check:
      run: npm run lighthouse:ci
      timeout: 300
```

### CI/CD интеграция (Phase 10)

```yaml
# .github/workflows/performance.yml
name: Performance Audit
on: [pull_request]

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - run: npm run build
      - run: npm start &
      - run: npx lhci autorun
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}
```

## 📚 Дополнительные ресурсы

### Документация

- **[BUNDLE_ANALYZER.md](./BUNDLE_ANALYZER.md)** — Детальный гайд по Bundle Analyzer
- **[LIGHTHOUSE.md](./LIGHTHOUSE.md)** — Полное руководство по Lighthouse CI
- **[RENOVATE.md](./RENOVATE.md)** — Мониторинг зависимостей

### Внешние ресурсы

- [Web Vitals](https://web.dev/vitals/) — Google documentation
- [Core Web Vitals](https://web.dev/vitals/#core-web-vitals) — Метрики и оптимизация
- [Next.js Performance](https://nextjs.org/docs/app/building-your-application/optimizing) — Официальные рекомендации
- [Bundle Size Best Practices](https://web.dev/fast/#optimize-your-js) — Google best practices

### Инструменты

- [Bundle Analyzer](https://www.npmjs.com/package/@next/bundle-analyzer) — NPM пакет
- [Lighthouse CI](https://github.com/GoogleChrome/lighthouse-ci) — GitHub repository
- [Vercel Analytics](https://vercel.com/analytics) — Real-time мониторинг
- [WebPageTest](https://www.webpagetest.org/) — Детальный анализ

## 🎓 Заключение

Регулярный мониторинг производительности — это инвестиция в качество продукта. Используя данный workflow, вы:

1. **Предотвращаете регрессы** — выявляете проблемы на ранних этапах
2. **Улучшаете UX** — пользователи получают быстрый и отзывчивый интерфейс
3. **Повышаете SEO** — Core Web Vitals влияют на ранжирование
4. **Снижаете технический долг** — оптимизация становится регулярной практикой

**Помните**: Оптимизация производительности — это марафон, а не спринт. Регулярные небольшие улучшения лучше, чем редкие кардинальные изменения.